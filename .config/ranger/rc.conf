## 1. rc.conf (~/.config/ranger/rc.conf)

**Fichier principal de configuration - Options et raccourcis clavier**

```conf
# ===== APPARENCE ET COMPORTEMENT =====
set viewmode miller
set column_ratios 1,3,4
set hidden_filter ^\.|\.(?:pyc|pyo|bak|swp)$|^lost\+found$|^__(py)?cache__$
set show_hidden true
set confirm_on_delete multiple
set preview_images true
set preview_images_method kitty
set use_preview_script true
set automatically_count_files true
set open_all_images true
set vcs_aware true
set vcs_backend_git enabled
set vcs_backend_hg disabled
set vcs_backend_bzr disabled
set draw_borders both
set dirname_in_tabs true
set mouse_enabled true
set display_size_in_main_column true
set display_size_in_status_bar true
set display_tags_in_all_columns true
set update_title true
set tilde_in_titlebar true
set max_history_size 20
set max_console_history_size 50
set scroll_offset 8
set flushinput true
set padding_right true
set autosave_bookmarks true
set save_console_history true
set status_bar_on_top false
set draw_progress_bar_in_status_bar true
set colorscheme default

# ===== PRÉVISUALISATION =====
set preview_files true
set preview_directories true
set collapse_preview true
set preview_max_size 0
set show_cursor false

# ===== NAVIGATION RAPIDE =====
map gh cd ~
map gd cd ~/Documents
map gD cd ~/Downloads
map gp cd ~/Pictures
map gv cd ~/Videos
map gm cd ~/Music
map gt cd /tmp
map gr cd /
map ge cd /etc
map gu cd /usr
map go cd /opt
map gv cd /var
map gl cd /var/log

# ===== MANIPULATION DE FICHIERS =====
map DD shell mv %s ~/.local/share/Trash/files/
map dD delete
map E  edit
map C  console rename%space
map A  eval fm.open_console('rename ' + fm.thisfile.relative_path.replace("%", "%%"))
map I  eval fm.open_console('rename ' + fm.thisfile.relative_path.replace("%", "%%"), position=7)

# Opérations basiques
map yy copy
map dd cut
map pp paste
map po paste overwrite=True
map pL paste_symlink relative=False
map pl paste_symlink relative=True
map phl paste hardlink
map pht paste hardlinked_subtree

# ===== SÉLECTION MULTIPLE =====
map <Space> mark_files toggle=True
map v       mark_files all=True toggle=True
map uv      mark_files all=True val=False
map V       toggle_visual_mode
map uV      unmark_files

# ===== CRÉATION =====
map mkd console mkdir%space
map mkf console touch%space

# ===== COMPRESSION =====
map ec shell for f in %s; do tar czf "$f.tar.gz" "$f"; done
map ez shell for f in %s; do zip -r "$f.zip" "$f"; done
map ex shell aunpack %s

# ===== RECHERCHE ET TRI =====
map f  console find%space
map /  console search%space
map n  search_next
map N  search_next forward=False

# Tri
map or set sort_reverse!
map oz set sort=random
map os chain set sort=size;      set sort_reverse=False
map ob chain set sort=basename;  set sort_reverse=False
map on chain set sort=natural;   set sort_reverse=False
map om chain set sort=mtime;     set sort_reverse=False
map oc chain set sort=ctime;     set sort_reverse=False
map oa chain set sort=atime;     set sort_reverse=False
map ot chain set sort=type;      set sort_reverse=False
map oe chain set sort=extension; set sort_reverse=False

# ===== ONGLETS =====
map <C-n>     tab_new
map <C-w>     tab_close
map <TAB>     tab_move 1
map <S-TAB>   tab_move -1
map gt        tab_move 1
map gT        tab_move -1
map gn        tab_new

# ===== TERMINAL =====
map S shell $SHELL

# ===== GIT =====
map gst shell git status
map gpl shell git pull
map gps shell git push
map gad shell git add %s
map gcm console shell git commit -m%space
map gdf shell git diff

# ===== PERMISSIONS =====
map +x shell chmod +x %s
map -x shell chmod -x %s
map +w shell chmod +w %s
map -w shell chmod -w %s

# ===== AFFICHAGE =====
map zh set show_hidden!
map zp set preview_files!
map zP set preview_directories!
map zv set use_preview_script!
map zi set preview_images!

# ===== OUVRIR AVEC =====
map o shell xdg-open %f &
map ow console open_with%space
map r chain draw_possible_programs; console open_with%%space

# ===== UTILITAIRES =====
map Y console shell cp -r %s%space
map bs console shell ln -s %f%space
map du shell du -sh %s | less
map df shell df -h | less

# ===== ALIAS =====
alias e     edit
alias q     quit
alias q!    quitall
alias qa    quitall
alias setl  setlocal
alias filter scout -prts
alias find   scout -aets
alias mark   scout -mr
alias unmark scout -Mr
alias search scout -rs
alias travel scout -aefklst
```
